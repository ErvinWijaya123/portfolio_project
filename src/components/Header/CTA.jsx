import React from "react";
// import cv_name from ...., and then, put the cv import into a brackets

const CTA = () => {
  return (
    <div className="cta">
      <a href={""} className="btn">
        Download CV
      </a>
      <a href="#contact" className="btn btn.primary">
        Let's Talk
      </a>
    </div>
  );
};

export default CTA;
